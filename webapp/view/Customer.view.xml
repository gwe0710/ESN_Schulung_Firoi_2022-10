<mvc:View controllerName="at.clouddna.student06.zhoui5.controller.Customer" xmlns:mvc="sap.ui.core.mvc" displayBlock="true" xmlns:l="sap.ui.layout" xmlns="sap.m" xmlns:f="sap.ui.layout.form" xmlns:core="sap.ui.core">
    <Page id="page" title="{i18n>title}">
        <customHeader>
            <OverflowToolbar id="app_toolbar">
                <Button id="customer_back_button" type="Back" press="onNavBack" visible="{= !${editModel>/editmode}}" />
                <Title id="app_toolbar_title" text="{i18n>title}" />
                <ToolbarSpacer id="toolbar_spacer_header"/>
                <Button id="app_edit_button" text="{i18n>edit}" type="Emphasized" icon="sap-icon://edit" press="onEditPressed" visible="{= !${editModel>/editmode}}" />
            </OverflowToolbar>
        </customHeader>
        <content>
            <!-- <core:Fragment id="app_change_fragment" fragmentName="at.clouddna.student06.zhoui5.view.fragment.ChangeCustomer" type="XML" /> -->
        </content>

        <footer>
            <Bar id="bar_footer">
                <contentRight>
                    <Button id="app_save_button" text="{i18n>save}" type="Accept" icon="sap-icon://save" press="onSavePressed" visible="{editModel>/editmode}" />
                    <Button id="app_cancel_button" text="{i18n>cancel}" type="Reject" icon="sap-icon://cancel" press="onCancelPressed" visible="{editModel>/editmode}" />
                </contentRight>
            </Bar>
        </footer>
    </Page>
</mvc:View>
